# Smart Motor Protocol V1

本文档定义 `pxt-smartmotor` 1.0.0 与 Smart Motor Ext Kit 固件之间的首个稳定兼容基线。除非后续协议版本明确说明，V1 实现必须保持本页中的地址、帧格式、命令编号、数据长度和数值编码不变。

## 总线与工作状态

- 总线：I2C，标准模式 100 kHz。
- 从机地址：7 位地址 `0x66`。不要把左移后的 8 位地址写入 MakeCode API。
- 固件只在扩展盒进入正式 `Working` 状态后响应 I2C。启动确认、关机提示和 USB 充电待机期间可能不应答。
- V1 控制命令没有“已接受”回复。一次 I2C 写入成功只表示数据已发送，不能证明电机命令已经入队或执行；参数无效、电机不可用或队列繁忙时命令可能被忽略。

## 请求帧

所有请求使用相同格式：

```text
FF F9 command length data...
```

| 字段 | 长度 | 说明 |
| --- | ---: | --- |
| `FF F9` | 2 字节 | 固定帧头 |
| `command` | 1 字节 | 命令编号 |
| `length` | 1 字节 | `data` 的字节数，不包含帧头、命令和长度字段 |
| `data` | `length` 字节 | 命令载荷；无载荷时 `length=0` |

V1 命令载荷最大为 20 字节。多字节运动参数在请求中使用大端序，具体格式见命令表；查询返回的传感器和电机数值使用小端序。

## 查询回复

查询命令先写入请求帧，再由主机读取：

```text
[valid, data...]
```

- 主机每次读取 `1 + expected_data_length` 字节。
- `valid=1` 表示本次 `data` 可用，主机可以解码并更新缓存。
- `valid=0` 表示数据尚未就绪。此时数据区可能包含该请求对象的旧快照，不能作为新结果，也不能覆盖最后一次有效缓存。
- 当前 PXT V1 实现每 2 ms 轮询一次，单次查询最长等待 100 ms。
- 查询超时必须记录为失败，并返回同一查询对象最后一次 `valid=1` 的缓存值；该对象从未成功读取过时，数值接口返回 0，版本接口返回 `V 0.0.0`。超时收到的旧数据或零数据不得写入缓存。
- 查询成功状态用于扩展内部诊断和异步操作确认，不增加或改变用户积木签名。

## 命令表

端口值为 `1..4`，对应 M1..M4。`motor_mask` 必须非零，并且只允许使用 bit0..bit3 表示 M1..M4；`data_mask` 只允许 `1..3`；`direction_mask` 只允许使用 bit0..bit1。速度百分比范围为 `0..100`，定量运动要求非零速度；`0x26` 和 `0x27` 的左右端口必须不同。

任何帧头、数据长度、数值范围、掩码、模式、方向、端口或寄存器范围不符合 V1 约束的帧都必须静默丢弃，不执行控制、不更新查询状态，也不返回错误回复。

| 命令 | 名称 | 请求数据 | 回复/语义 |
| ---: | --- | --- | --- |
| `0x00` | 寄存器总长度 | 空，0 字节 | 查询命令；回复 `[valid, register_count]`。V1 当前 `register_count=0x4D`，因此有效寄存器范围为 `0x00..0x4C`。 |
| `0x01` | 寄存器读取 | `[start, count]`，2 字节 | 查询命令；回复 `[valid, count bytes]`。`count` 为 `1..24`，范围不得超过 V1 寄存器表 `0x00..0x4C`。 |
| `0x02` | 电机数据刷新 | `[port, data_mask]`，2 字节 | 查询命令；回复 `[valid, 13-byte motor record]`。`data_mask` 只能为 `1..3`：bit0 请求角度，bit1 请求速度。 |
| `0x10` | 固件版本 | 空，0 字节 | 查询命令；回复 `[valid, major, minor, patch]`。 |
| `0x11` | 陀螺仪累计角度复位 | 空，0 字节 | 控制命令；异步请求在后续完整传感器采样边界清零 X/Y/Z，不返回确认。 |
| `0x20` | 单电机设置速度 | `[port, speed_percent, direction]`，3 字节 | `direction`：0 顺时针，1 逆时针。 |
| `0x21` | 停止电机 | `[motor_mask]`，1 字节 | 最高优先级停止掩码指定的电机，并取消其尚未执行的运动命令。 |
| `0x22` | 单电机定量运动 | `[port, mode, value_be32, speed_percent, direction]`，8 字节 | `mode`：1 圈（发送前换算为角度），2 角度，3 时间；见下方数值说明。方向为 0/1。 |
| `0x23` | 单电机绝对角度 | `[port, target_be16, speed_percent, turn]`，5 字节 | `target` 为 `0..3599`，单位 0.1 度；`turn`：0 顺时针，1 逆时针，2 最短路径。 |
| `0x24` | 物理位置归零 | `[motor_mask]`，1 字节 | 将指定电机当前位置设为物理零点。 |
| `0x25` | 相对角度归零 | `[motor_mask]`，1 字节 | 将指定电机当前位置保存为下位机相对零点。 |
| `0x26` | 双电机设置速度 | `[left_port, right_port, left_speed, right_speed, direction_mask]`，5 字节 | 左右端口必须不同。`direction_mask` 只能使用 bit0/bit1，分别控制左轮/右轮方向；置 1 为逆时针。两路作为一个命令整体处理。 |
| `0x27` | 双电机定量运动 | `[left_port, right_port, mode, left_value_be32, right_value_be32, speed_percent, direction_mask]`，13 字节 | 左右端口必须不同。模式和数值单位同 `0x22`；`direction_mask` 只能使用 bit0/bit1，分别控制左轮/右轮方向。两路作为一个命令整体处理。 |

`0x22` 和 `0x27` 的 `value` 是无符号大端 32 位数：

- `mode=1` 或 `mode=2`：单位为 0.1 度。PXT 的“圈”接口在发送前将圈数换算为角度，例如 1 圈发送 `3600`。
- `mode=3`：单位为 0.1 秒，`value` 最大为 `65535`，即最长 `6553.5` 秒；例如 2.5 秒发送 `25`。
- 正反方向由独立的 `direction` 或 `direction_mask` 字段表示，`value` 本身不带符号。

## 寄存器表

`0x01` 返回连续字节。下表中的多字节值均为有符号小端整数，版本和状态字节除外。

| 地址 | 长度 | 类型 | 含义 |
| ---: | ---: | --- | --- |
| `0x00` | 3 | `u8[3]` | 固件 `major.minor.patch` |
| `0x03` | 4 | `i32 LE` | 陀螺仪 X 轴累计角度，单位 0.1 度 |
| `0x07` | 4 | `i32 LE` | 陀螺仪 Y 轴累计角度，单位 0.1 度 |
| `0x0B` | 4 | `i32 LE` | 陀螺仪 Z 轴累计角度，单位 0.1 度 |
| `0x0F` | 2 | `i16 LE` | X 轴加速度，单位 mg |
| `0x11` | 2 | `i16 LE` | Y 轴加速度，单位 mg |
| `0x13` | 2 | `i16 LE` | Z 轴加速度，单位 mg |
| `0x15` | 4 | `u8[4]` | M1..M4 最近一次通信错误码 |
| `0x19` | 13 | 记录 | M1 电机数据 |
| `0x26` | 13 | 记录 | M2 电机数据 |
| `0x33` | 13 | 记录 | M3 电机数据 |
| `0x40` | 13 | 记录 | M4 电机数据 |

错误码：0 无错误，1 心跳超时，2 命令回复超时，3 发送超时。

## 13 字节电机记录

`0x02` 的数据区以及寄存器表中的每路电机记录使用相同布局：

| 偏移 | 长度 | 类型 | 含义 |
| ---: | ---: | --- | --- |
| 0 | 1 | `u8` | flags：bit0 角度有效，bit1 速度有效 |
| 1 | 4 | `i32 LE` | 相对角度，单位 0.1 度 |
| 5 | 4 | `i32 LE` | 单圈绝对角度，范围 `0..3599`，单位 0.1 度 |
| 9 | 2 | `i16 LE` | 速度，单位 度/秒 |
| 11 | 1 | `u8` | 角度更新序列号，按 8 位自然回绕 |
| 12 | 1 | `u8` | 速度更新序列号，按 8 位自然回绕 |

数据使用方应同时检查查询回复的 `valid` 和记录内部对应的 flags。序列号用于判断是否取得了新样本，不能只比较数值是否变化。

## 陀螺仪复位弱确认

`0x11` 只发布复位请求。固件在下一次成功读取到完整 QMI8658 样本时，于三轴一致的采样边界执行清零，因此命令发送后立即读取可能仍得到复位前角度。

PXT V1 使用启发式弱确认：发送 `0x11` 后，在约 350 ms 的有限窗口内轮询三轴陀螺仪角度，只把 `valid=1` 的完整新回复用于判断；当 X、Y、Z 三轴都落在 `-0.1..+0.1` 度范围内时提前返回。查询超时返回的缓存值不能视为复位完成。

这种判断不是硬 ACK：如果复位前角度本来就接近零，轮询可能在固件真正处理复位请求前提前返回。V1 没有独立的复位状态位或复位序列号，主机无法严格区分“复位已经执行”和“角度恰好接近零”；需要硬确认时，应在后续兼容扩展中新增状态字段，不能改变 `0x11` 的现有含义。

## V1 兼容性规则

- 已有命令编号、请求载荷和回复布局保持稳定。
- 新命令应使用未占用编号；新增寄存器应追加在现有寄存器表之后。
- 不得把 `valid=0` 重新解释为成功，也不得用其数据更新 PXT 缓存。
- 扩展可以增强内部超时、重试和确认逻辑，但不得因此改变公开积木的参数、返回类型或既有单位。
